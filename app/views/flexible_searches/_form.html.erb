<%= form_with(model: flexible_search, local: true) do |form| %>
  <% if flexible_search.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(flexible_search.errors.count, "error") %> prohibited this flexible_search from being saved:</h2>

      <ul>
        <% flexible_search.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>

    
<% end %>
