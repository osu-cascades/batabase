<%= render_component('index-header', { resource: 'Contact', path:  new_contact_path }) %>

<%= render_component(
  'index-table',
  { resources: @contacts,
    headers: [:name, :email, :organization, :state_code] })
%>
