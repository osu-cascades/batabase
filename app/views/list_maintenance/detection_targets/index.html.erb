<%= render_component('page') do %>
  <%= render_component(
    'index-header',
    { resource: 'Detection Target',
      path:  new_detection_target_path }
  )%>

  <%= render_component(
    'index-nested-table',
    { resources: @detection_targets,
      sub_resources: 'target_descriptors',
      attributes: [{ label: :label }] }
  )%>

<% end %>
