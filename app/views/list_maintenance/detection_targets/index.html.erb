<%= render_component('index-header', { resource: 'Detection Target', path:  new_detection_target_path }) %>

<%= render_component('index-table', { resources: @detection_targets, headers: [:label] }) %>
