<%= render_component('index-header', { resource: 'Detectors', path:  new_detector_path }) %>

<%= render_component(
  'index-table',
  { resources: @detectors,
    headers: [:serial_num, :model, :manufacturer, :firmware, :owning_agency] })
%>
