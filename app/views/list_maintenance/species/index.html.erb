<%= render_component('index-header', { resource: 'Species', path:  new_species_path }) %>

<%= render_component('index-table',
  { resources: @species,
  headers: [:code, :common_name, :scientific_name] })
%>
